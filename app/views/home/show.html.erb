<%= render(Layout::AppComponent.new) do %>
  <div>
    <section class="flex max-w-screen-md mx-auto">
      <div class="p-8">
        <h2 class="text-2xl font-bold">
          <%= @blog.title %>
        </h2>
        <%= image_tag @blog.featured_image.variant(:large) %>
        <div class="prose">
          <%= @blog.content %>
        </div>
      </div>
    </section>
  </div>
<% end %>
